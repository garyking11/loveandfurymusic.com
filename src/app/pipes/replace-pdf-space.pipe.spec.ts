import { ReplacePdfSpacePipe } from './replace-pdf-space.pipe';

describe('ReplacePdfSpacePipe', () => {
  it('create an instance', () => {
    const pipe = new ReplacePdfSpacePipe();
    expect(pipe).toBeTruthy();
  });
});
