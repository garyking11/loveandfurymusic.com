import { AddDashGtr1Pipe } from './add-dash-gtr1.pipe';

describe('AddDashGtr1Pipe', () => {
  it('create an instance', () => {
    const pipe = new AddDashGtr1Pipe();
    expect(pipe).toBeTruthy();
  });
});
