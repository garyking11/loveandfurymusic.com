import { SearchRecursivePipe } from './search-recursive.pipe';

describe('SearchRecursivePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchRecursivePipe();
    expect(pipe).toBeTruthy();
  });
});
